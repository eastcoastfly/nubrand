(function ($, window, document) {
	//
	// ... code out here will run immediately on load
	//

	$(function () {
		//
		// ? start of $.ready
	
		// ? end of $.ready
		//
	});
})(window.jQuery, window, document);